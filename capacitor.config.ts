import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kidsaibookspoc.app',
  appName: 'kids ai books',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
